# تثبيت v126

- ارفع ملفات النسخة كاملة، وبالأخص `index.html` و`js/main.js`.
- نفّذ تحديثًا إجباريًا للمتصفح `Ctrl + F5` بعد الرفع.
- ستظهر صلاحية `Commission 🚩` في كارت مستقل أسفل صفحة Permission بجوار كارت `أنواع الإلغاء`.
- اختر الـRole، فعّل `عرض صفحة Commission 🚩`، ثم اضغط `حفظ الصلاحيات`.

## Supabase

- إذا لم يتم تشغيل Migration 019 من قبل، شغّل `supabase/019_role_commission_branch_stock_lock.sql` مرة واحدة باستخدام `Run without RLS`.
- إذا تم تشغيله بالفعل بنجاح، فلا حاجة لتشغيله مرة أخرى بسبب هذا التعديل؛ v126 غيّر موضع العرض فقط ولم يغيّر SQL.

الصلاحية محفوظة على مستوى الـRole، وبيانات غير الـAdmin تظل محصورة في الفروع المرتبطة بالحساب.
