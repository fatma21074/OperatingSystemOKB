# تثبيت المزامنة اللحظية — OKB CRM

1. افتح Supabase ثم SQL Editor ثم New Query.
2. انسخ وشغّل الملف `supabase/008_live_business_sync.sql` مرة واحدة.
3. يجب أن تُظهر نتيجة الفحص أربعة جداول: `items`, `orders`, `role_permissions`, `user`.
4. ارفع ملفات المشروع على فرع GitHub المستخدم للنسخة الأونلاين.
5. نفّذ تحديثًا قويًا `Ctrl + F5` على جهازين للاختبار.

## اختبار سريع

- من الجهاز الأول عدّل ملاحظة أو سعر أوردر اختبار.
- تأكد أن الجهاز الثاني حدّث الصف والكروت المفتوحة خلال لحظات.
- غيّر صلاحية Role وتأكد أن الزر يظهر أو يختفي على الجهاز الثاني.
- عدّل سعر منتج وتأكد أن قائمة المنتجات تعرض السعر الجديد للأوردرات الجديدة.

لا يضيف ملف SQL أعمدة ولا يعدّل أي أوردر أو قيمة مالية.
