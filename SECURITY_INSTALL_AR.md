# تثبيت سور حماية OKB بدون تغيير الحسابات

هذا التحديث لا يضيف إيميلات، ولا يغيّر أسماء المستخدمين أو كلمات المرور أو شاشة الدخول.

## مهم جدًا

لا تشغّل ملف `003_security_lockdown.sql` قبل اختبار بوابة الدخول الجديدة؛ لأنه يقفل الوصول العام للجداول فعليًا.

## الترتيب الصحيح

1. خذ نسخة احتياطية من قاعدة البيانات.
2. افتح Supabase > SQL Editor > New query.
3. شغّل محتوى `supabase/002_security_prepare.sql`.
4. تأكد أن نتيجة آخر استعلام تعرض نفس الرقم في `total_users` و`protected_passwords`.
5. من Supabase > Project Settings > API Keys > Legacy JWT Secret، انسخ السر داخل إعدادات Edge Function فقط باسم `OKB_JWT_SECRET`.
   - لا تضع السر في `main.js`.
   - لا ترفعه على GitHub.
   - لا ترسله في المحادثة.
6. انشر Edge Function الموجودة في `supabase/functions/okb-login` مع تعطيل Verify JWT لهذه الدالة فقط.
7. انشر ملفات الواجهة المعدلة واختبر تسجيل الدخول بحساب عادي وحساب Admin، ثم اختبر Activity Log وChat والبيانات اللحظية.
8. بعد نجاح الاختبار فقط، شغّل `supabase/003_security_lockdown.sql`.
9. افتح Security Advisor واضغط Rerun Linter.

## النتيجة

- نفس Username وPassword القديمين للمستخدم.
- كلمة المرور لا تُقرأ من المتصفح ولا تُخزّن كنص واضح بعد القفل النهائي.
- كل جلسة تحصل على تصريح مؤقت مدته 12 ساعة.
- الجداول لا تعمل بالمفتاح العام وحده.
- Realtime يعمل بنفس تصريح الجلسة الآمن.

## رجوع طارئ قبل المرحلة النهائية

ملف التحضير `002` لا يقفل الجداول، ولذلك لا يحتاج رجوعًا عند توقف نشر الدالة. لا تشغّل `003` إلا بعد نجاح الاختبار الكامل.
