# تثبيت v127

## سبب التصحيح

رسالة `Commission غير مضافة لصلاحيات حسابك` صادرة من Migration 018 القديمة التي كانت تفحص صلاحية فردية للمستخدم.

## خطوات التثبيت

1. ارفع ملفات v127 كاملة.
2. شغّل `supabase/020_commission_role_enforcement.sql` مرة واحدة باستخدام `Run without RLS`.
3. افتح Permission، اختر الـRole، فعّل `عرض صفحة Commission 🚩` واضغط `حفظ الصلاحيات`.
4. تأكد من ربط المستخدم بفرع واحد أو أكثر من صفحة Users.
5. نفّذ `Ctrl + F5` ثم سجّل دخول المستخدم من جديد.

Migration 020 آمنة بعد 018 أو 019؛ تستبدل فقط دالة قراءة بيانات Commission لتفحص صلاحية الـRole، ولا تغيّر معادلات العمولة أو بيانات الأوردرات أو المخزون.
