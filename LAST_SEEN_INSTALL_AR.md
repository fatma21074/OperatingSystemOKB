# تثبيت Last Seen الحقيقي

1. افتح Supabase ثم **SQL Editor** ثم **New query**.
2. انسخ محتوى الملف `supabase/007_user_last_seen.sql` بالكامل.
3. اضغط **Run** مرة واحدة فقط.
4. تأكد أن النتيجة تعرض العمود `last_seen_at`.
5. بعد ذلك ارفع ملفات النسخة الجديدة على الموقع.

## الحالات

- `Online` أخضر: المستخدم متصل حاليًا.
- `Last Seen: التاريخ والوقت` أصفر: المستخدم غير متصل منذ أقل من 24 ساعة.
- `Offline` أحمر: آخر اتصال أقدم من 24 ساعة أو لم يسجل الدخول منذ تثبيت النظام.

لا يعتمد هذا النظام على Activity Log. يتم حفظ نبضة اتصال آمنة كل 60 ثانية، وكل مستخدم يستطيع تحديث وقت ظهوره الخاص فقط.
