# تشغيل Branch Stock v104

1. افتح Supabase ثم SQL Editor.
2. شغّل الملف `supabase/011_branch_stock_control.sql` مرة واحدة فقط.
3. شغّل الملف `supabase/012_branch_stock_settlement_confirmation.sql` مرة واحدة فقط.
4. شغّل الملف `supabase/013_branch_stock_receipt_resend.sql` مرة واحدة فقط.
5. شغّل الملف `supabase/014_branch_stock_pending_edit.sql` مرة واحدة فقط.
6. شغّل الملف `supabase/015_branch_stock_bulk_add.sql` مرة واحدة فقط.
7. شغّل الملف `supabase/016_branch_signed_deduction_control.sql` مرة واحدة فقط.
8. افتح صفحة Permission من حساب Admin.
9. امنح `➕ عمود إضافة رصيد` للحسابات أو للمستخدم المطلوب.
10. امنح `✅❌ عمود الإجراءات` لمدير الفرع أو للمستخدم المطلوب.
11. أضف الرصيد الافتتاحي للمنتجات ثم أكّد استلامه قبل تحويل أوردرات جديدة إلى Signed.

## قواعد التشغيل

- إضافة الرصيد تظل معلقة ولا تغيّر المخزون قبل استلامها.
- ✅ يقبل الاستلام المطابق فقط.
- ❌ يسجل الاختلاف أو الهالك، وسببه إلزامي.
- الإجمالي الفعلي = المستلم - الهالك.
- نفس المستخدم لا يستطيع إضافة الرصيد واستلامه، باستثناء Admin.
- أول انتقال للأوردر إلى Signed يخصم المنتجات مرة واحدة فقط.
- إذا كان الرصيد غير كافٍ أو المنتج غير موجود، لا يتم تحويل الأوردر إلى Signed.
- كل حركة تظهر داخل Activity Log.
- بعد استلام مدير الفرع، يظهر لمدير الحسابات والـAdmin داخل عمود `سبب الفرق` ملخص المضاف والمستلم والهالك والإجمالي الفعلي والفرق واسم المستلم.
- الاستلام المطابق يظهر بإطار Neon أخضر، وأي عجز أو زيادة أو هالك يظهر بإطار Neon أحمر حتى تتم التسوية.
- بعد ضغط مدير الحسابات أو Admin على `تأكيد` يختفي ملخص التفاصيل ويظل سطر الحالة المختصر فقط.
- زر `إرسال مرة أخرى` يلغي أثر الاستلام غير المؤكد من الرصيد ثم يعيد الطلب إلى Store Manager لإدخال الأعداد الصحيحة.
- زر `تعديل` متاح للـAdmin ومدير الحسابات على الطلب المعلق فقط، ويعدل الكمية قبل استلام مدير الفرع مع تسجيل السبب.
- يمكن تحديد عدة منتجات وإرسال كميات مختلفة لها في عملية واحدة، وسبب إضافة الرصيد اختياري في الإضافة الفردية والجماعية.
- يستطيع Admin إيقاف أو تشغيل خصم Signed للفرع المحدد فقط. أثناء الإيقاف تمر الأوردرات إلى Signed بلا خصم وتُحفظ في سجل انتظار التسوية.
