# تثبيت v129

1. ارفع ملفات v129 كاملة.
2. شغّل `supabase/021_commission_approval_role.sql` مرة واحدة باستخدام `Run without RLS`.
3. من Permission اختر الـRole، ثم فعّل:
   - `عرض صفحة Commission 🚩`
   - `اعتماد العمولة 🔒`
4. اربط حساب الشخص المفوض بالفروع الأربعة من Users لأن الاعتماد شهري شامل لكل الفروع.
5. اضغط حفظ الصلاحيات، ثم نفّذ Logout / Login للحساب المفوض.

صلاحية فتح الاعتماد بعد إقفاله تظل للـAdmin فقط. Migration 021 لا تغيّر معادلات العمولة أو بيانات الأوردرات.
